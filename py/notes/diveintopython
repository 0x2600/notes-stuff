import os
os.getcwd()
os.chdir()

os.path.join()
os.path.expanduser('~')
(dirname, filename) = os.path.split(/dir1/file.txt)
(shortname, extension) = os.path.splitext(filename)
os.path.realpath()

import glob
glob.glob('examples/*.xml')

metadata = os.stat('feed.xml')
metadata.st_mtime
import time
time.localtime(metatime.st_mtime)
metadata.st_size 

